<!--             @if ($photo->count())

                @foreach ($photo as $photo)
                    <img src="{{ $photo->src('300x200') }}">
                @endforeach

            @else    
                <center>not found images</center>
            @endif
 -->


  
